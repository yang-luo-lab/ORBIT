% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ORBIT_simulate_signal.R
\name{ORBIT_simulate_signal}
\alias{ORBIT_simulate_signal}
\title{Simulate Multi-Omic Mixture Data with Up- and Down-Regulated Signals}
\usage{
ORBIT_simulate_signal(
  n = 1000,
  K = 2,
  pi_null = 0.8,
  pi_up = 0.1,
  pi_down = 0.1,
  mu_signal = 2,
  rho_null = 0,
  rho_signal = 0,
  seed = NULL
)
}
\arguments{
\item{n}{Number of features to simulate. Must be a positive
integer.}

\item{K}{Number of omics. Must be at least 2.}

\item{pi_null}{Mixture proportion of null features. Must be in
\verb{[0, 1]} and \code{pi_null + pi_up + pi_down} must equal 1.}

\item{pi_up}{Mixture proportion of up-regulated features.}

\item{pi_down}{Mixture proportion of down-regulated features.}

\item{mu_signal}{Mean of the multivariate normal \code{Z} for up-
regulated features (down-regulated features use \code{-mu_signal}).
Larger values produce stronger signals.}

\item{rho_null}{Average pairwise correlation between omics among
null features. Must lie in \verb{(-1 / (K - 1), 1)}. Defaults to \code{0}.}

\item{rho_signal}{Average pairwise correlation between omics
among signal features (up and down combined). Must lie in
\verb{(-1 / (K - 1), 1)}. Defaults to \code{0}.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL}
(default), the current random state is used.}
}
\value{
A list with components:
\describe{
\item{\code{omics_list}}{Named list of length \code{K}, one data frame
per omic, with columns \code{feature} (character), \code{sign}
(\code{+1} / \code{-1}), and \code{stat} (two-sided P-value). Names are
\code{omic1} ... \code{omicK}. Ready to pass directly to \code{\link[=ORBIT_P]{ORBIT_P()}},
\code{\link[=ORBIT_Rank]{ORBIT_Rank()}}, or \code{\link[=ORBIT_cor]{ORBIT_cor()}}.}
\item{\code{Z_mat}}{The underlying \code{n} by \code{K} normal draws, with
row names \code{feature1} ... \code{featureN} and column names
\code{omic1} ... \code{omicK}.}
\item{\code{is_signal}}{Logical vector of length \code{n}, named by
feature. \code{TRUE} for up- or down-regulated features, \code{FALSE}
for null features.}
\item{\code{group}}{Character vector of length \code{n}, named by
feature, with values \code{"null"}, \code{"up"}, or \code{"down"}
indicating each feature's true class.}
\item{\code{Sigma_null}}{The \code{K} by \code{K} exchangeable correlation
matrix used for null features.}
\item{\code{Sigma_signal}}{The \code{K} by \code{K} exchangeable correlation
matrix used for signal features.}
}
}
\description{
Generates a synthetic multi-omic dataset as a three-component
mixture of null, up-regulated, and down-regulated features.
Within each component, a length-\code{K} multivariate normal vector is
drawn for each feature (with a configurable between-omic
correlation), then converted to two-sided P-values and signs. The
output is in the \code{omics_list} format expected by \code{\link[=ORBIT_P]{ORBIT_P()}},
\code{\link[=ORBIT_Rank]{ORBIT_Rank()}}, and \code{\link[=ORBIT_cor]{ORBIT_cor()}}, so it can be fed directly to
any of those without further conversion.
}
\details{
For each feature, a label is drawn from \code{c("null", "up", "down")}
with probabilities \code{c(pi_null, pi_up, pi_down)}. Null features
have mean-zero normal \code{Z}; up-regulated features have mean
\code{+mu_signal}; down-regulated features have mean \code{-mu_signal}. The
between-omic correlation is set to \code{rho_null} for null features
and \code{rho_signal} for signal features (up and down combined). All
random draws use base R only, so no external packages are
required for the simulation.
}
\examples{
# 80\% null, 10\% up, 10\% down, with between-omic correlation
sim <- ORBIT_simulate_signal(
  n = 500, K = 3,
  pi_null = 0.8, pi_up = 0.1, pi_down = 0.1,
  mu_signal = 2,
  rho_null = 0.2, rho_signal = 0.5,
  seed = 1
)

# Returned structure: list of per-omic data frames + ground-truth labels
names(sim)
head(sim$omics_list[[1]])
table(sim$is_signal)
}
