% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ORBIT_simulate_null.R
\name{ORBIT_simulate_null}
\alias{ORBIT_simulate_null}
\title{Simulate Multi-Omic Null Data with Specified Between-Omic Correlation}
\usage{
ORBIT_simulate_null(
  n = 1000,
  K = 2,
  rho = 0,
  missing_rate = 0,
  missing_omics = "all",
  seed = NULL
)
}
\arguments{
\item{n}{Number of features to simulate. Must be a positive
integer.}

\item{K}{Number of omics. Must be at least 2.}

\item{rho}{Average pairwise correlation between omics, in the
open interval \verb{(-1 / (K - 1), 1)}. Defaults to \code{0}
(independent omics).}

\item{missing_rate}{Fraction of features dropped from each target
omic. Must be in \verb{[0, 1)}. Defaults to \code{0} (no missingness).
Missingness is independent across target omics (MCAR).}

\item{missing_omics}{Which omics to apply missingness to. Either
the string \code{"all"} (every omic, default), the string \code{"last"}
(only the last omic), or an integer vector of omic indices in
\code{1:K}. Ignored when \code{missing_rate = 0}.}

\item{seed}{Optional integer seed for reproducibility. If \code{NULL}
(default), the current random state is used.}
}
\value{
A list with components:
\describe{
\item{\code{omics_list}}{Named list of length \code{K}, one data frame
per omic, with columns \code{feature} (character), \code{sign}
(\code{+1} / \code{-1}), and \code{stat} (two-sided P-value). Names are
\code{omic1} ... \code{omicK}. Ready to pass directly to \code{\link[=ORBIT_P]{ORBIT_P()}},
\code{\link[=ORBIT_Rank]{ORBIT_Rank()}}, or \code{\link[=ORBIT_cor]{ORBIT_cor()}}. Features dropped by
\code{missing_rate} simply do not appear in the corresponding
data frame.}
\item{\code{Z_mat}}{The underlying \code{n} by \code{K} standard normal
draws, with row names \code{feature1} ... \code{featureN} and column
names \code{omic1} ... \code{omicK}. Entries corresponding to dropped
features are \code{NA} so callers can recover which features
were removed from each omic.}
\item{\code{Sigma}}{The \code{K} by \code{K} exchangeable correlation matrix
used for sampling.}
}
}
\description{
Generates a synthetic multi-omic dataset under the null hypothesis:
all features are non-significant, but P-values across omics are
allowed to share a common pairwise correlation \code{rho}. The output
is in the \code{omics_list} format expected by \code{\link[=ORBIT_P]{ORBIT_P()}},
\code{\link[=ORBIT_Rank]{ORBIT_Rank()}}, and \code{\link[=ORBIT_cor]{ORBIT_cor()}}, so it can be fed directly to
any of those without further conversion. Optional MCAR
missingness can be injected by dropping features from selected
omics' data frames.
}
\details{
Internally, a length-\code{K} standard multivariate normal vector is
drawn for each feature using a Cholesky decomposition of the
exchangeable correlation matrix (1 on the diagonal, \code{rho} off the
diagonal). Each component is converted to a two-sided normal
P-value, with the corresponding sign retained. If \code{missing_rate}
is positive, a fraction of features in each target omic is
removed from that omic's data frame entirely (completely at random
within each targeted omic). No external packages are required for
the simulation.
}
\examples{
# Independent null, ready to feed into ORBIT_P
sim <- ORBIT_simulate_null(n = 200, K = 3, rho = 0, seed = 1)
direction <- setNames(rep(1, 3), names(sim$omics_list))
res <- ORBIT_P(sim$omics_list, direction)
head(res)

# Correlated null
sim2 <- ORBIT_simulate_null(n = 200, K = 4, rho = 0.4, seed = 1)
length(sim2$omics_list)

# Half of features missing in the last omic only
sim3 <- ORBIT_simulate_null(n = 200, K = 2, rho = 0.3,
                            missing_rate = 0.5,
                            missing_omics = "last",
                            seed = 1)
sapply(sim3$omics_list, nrow)
}
