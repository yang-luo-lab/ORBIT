% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ORBIT_Rank.R
\name{ORBIT_Rank}
\alias{ORBIT_Rank}
\title{Variance-Gamma Integration of Multi-Omic Rank Statistics}
\usage{
ORBIT_Rank(
  omics_list,
  direction,
  rho = 0,
  stat_direction = c("smaller_better", "larger_better"),
  feature_col = "feature",
  sign_col = "sign",
  stat_col = "stat",
  seed = NULL
)
}
\arguments{
\item{omics_list}{Named list of data frames, one per omic. Each must
contain three columns whose names are given by \code{feature_col},
\code{sign_col}, \code{stat_col}. Different omics may report different
subsets of features; the function works on the union.}

\item{direction}{Named numeric vector of +1/-1, names must match
\code{names(omics_list)}. Multiplied into the score so that the user
can flip an omic's sign convention without editing \code{sign_col}.}

\item{rho}{Between-omic correlation. Either:
\itemize{
\item A single number in \verb{(-1/(k_max - 1), 1)}, where \code{k_max} is the
largest number of omics in which any single feature is
observed. The same value is used for every feature. For two
omics (k_max = 2) any value in \verb{(-1, 1)} is allowed. Negative
\code{rho} shrinks the null variance and increases power for
direction-aligned signals (e.g. testing reversal between two
contrasts); positive \code{rho} accounts for shared technical /
batch nuisance correlation.
\item A numeric square symmetric matrix with \code{rownames} and
\code{colnames} matching \code{names(omics_list)} (order may differ; it
is reordered internally). Diagonal must be exactly 1; off-
diagonal entries must be strictly less than 1. \code{NA} is allowed
in off-diagonal entries for pairs whose correlation could not
be estimated (see "Matrix mode" below for handling).
Default \code{0}.
}}

\item{stat_direction}{Convention for \code{stat_col}. \code{"smaller_better"}
(default) treats smaller \code{stat} as more significant (p-value
style); \code{"larger_better"} treats larger \code{stat} as more
significant (\verb{|t-stat|}, \code{-log10(p)}, enrichment score, etc.).
Either way \code{stat} must be non-negative; the sign of the effect
belongs in \code{sign_col}.}

\item{feature_col, sign_col, stat_col}{Column names inside each data
frame. Defaults: \code{"feature"}, \code{"sign"}, \code{"stat"}. \code{feature} may
be character or factor (factors are coerced); \code{sign} must be -1
or +1 (no NA); \code{stat} must be numeric, non-negative, with no NA.}

\item{seed}{Optional seed for random tie-breaking in ranking. The
global RNG state is restored on exit.}
}
\value{
Data frame with columns:
\itemize{
\item \code{Feature}: feature identifier (union across omics).
\item \code{N}: integer, number of omics in which the feature is observed
(i.e. has non-NA score). \code{0} is possible only if a feature
appears in some omic but with no rows — in practice every
returned feature has \code{N >= 1}.
\item \code{P}: two-sided Variance-Gamma tail probability. \code{NA} when
\code{N == 0}, or (matrix mode only) when the feature's effective
inflation is non-positive.
\item \code{Direction}: integer, \code{1L} if \code{D > 0}, \code{-1L} if \code{D < 0}, \code{NA}
if \code{D == 0} or \code{P} is \code{NA}.
}
}
\description{
Combines per-omic signed rank statistics into a single
per-feature p-value via a Variance-Gamma null calibrated by the
average between-omic correlation. The correlation can be supplied
as a single scalar or as a full pairwise matrix; in the matrix
case the effective correlation is computed per feature from the
subset of omics in which that feature is observed.
}
\details{
\strong{Score construction.} Within each omic, features are ranked by
\code{stat} (most-significant first, controlled by \code{stat_direction})
with random tie-breaking. The per-feature signed score is
\code{sign * direction * (-log(adj_rank))}, where
\code{adj_rank = r / (m + 1)} and \code{m} is the number of features in that
omic. Under the null \verb{|score| ~ Exp(1)} and the signed score is
marginally Laplace(0, 1). A feature not observed in a given omic
contributes nothing to its combined statistic.

\strong{Combined statistic.} For each feature, \verb{D = sum over omics of score}, taken over the \code{k} omics in which the feature is observed.
\code{N} (returned) is this \code{k}. Under the equicorrelated Laplace null
with correlation \code{eff_rho}, \code{D} follows a symmetric Variance-Gamma
distribution; the correlation inflation \code{1 + (k - 1) * eff_rho} is
absorbed via moment matching (variance exactly; higher moments
approximate). The inflation must be strictly positive, which
requires \code{eff_rho > -1/(k - 1)}.

\strong{Scalar mode.} \code{eff_rho = rho} for every feature. The function
computes \code{k_max = max(N)} over features with \code{N > 0} and enforces
\code{rho > -1/(k_max - 1)} (with a small numerical tolerance);
violation is an error. When \code{rho < 0} and \code{k_max >= 2}, a message
notes that the negative value is within the valid range and
increases power for direction-aligned signals.

\strong{Matrix mode.} Each feature has its own \emph{presence pattern} (the
set of omics in which it is observed). Features are grouped by
pattern and assigned \code{eff_rho} = the mean of the upper-triangular
entries of \code{rho} restricted to the omics in that pattern. Single-
omic features (\code{k = 1}) get \code{eff_rho = 0} as a placeholder, since
\code{(k - 1) * eff_rho = 0} regardless. Handling of \code{NA} entries in
\code{rho}:
\itemize{
\item If the relevant submatrix has \emph{some} \code{NA} pairs but at least
one finite pair, \code{eff_rho} is the mean of the finite pairs and
a single \code{warning()} reports the count.
\item If \emph{all} relevant pairs are \code{NA}, \code{eff_rho} falls back to \code{0}
and the same warning reports this count separately.
\item If the resulting inflation \code{1 + (k - 1) * eff_rho} is not
strictly positive (within \code{1e-6}), the feature is skipped:
\code{eff_rho} stays \code{NA} and the feature's returned \code{P} is \code{NA}.
A separate warning reports the count.
In matrix mode a message also reports the median and range of
per-feature \code{eff_rho} over features with \code{N >= 2}.
}

\strong{Tail probability.} Per-feature \verb{P = P(|D'| >= |D|)} is computed
on the Variance-Gamma with shape \code{r_eff = k / (1 + (k-1)*eff_rho)}
and scale \code{sigma_eff = 1 + (k-1)*eff_rho}, evaluated by 32-node
Gauss-Laguerre quadrature on the upper tail in log space. Special
cases: \code{r_eff = 1} (Laplace) and \code{r_eff = 2} use closed forms.
Features are grouped by \verb{(N, eff_rho)} so the quadrature setup is
reused.

\strong{Reported feature overlap.} Before scoring, the function emits
\code{message()}s with the size of the feature union, the number and
percentage of features shared across \emph{all} omics, and a note
listing any omic pair with zero shared features (informational; it
does not affect this function but may be relevant to the choice of
\code{rho} if \code{rho} was estimated from such a pair).
}
\examples{
# 500 features, 80\% null, 20\% true signal (10\% up + 10\% down)
sim <- ORBIT_simulate_signal(
  n = 500, K = 3,
  pi_null = 0.8, pi_up = 0.1, pi_down = 0.1,
  mu_signal = 2,
  rho_null = 0, rho_signal = 0.5,
  seed = 1
)
direction <- setNames(rep(1, 3), names(sim$omics_list))

# Independence assumption: rho = 0 (matches rho_null here)
res <- ORBIT_Rank(sim$omics_list, direction, rho = 0, seed = 1)
head(res)

# Calibration sanity check against the simulated truth
fdr   <- p.adjust(res$P, method = "BH")
truth <- sim$is_signal[res$Feature]
table(predicted = fdr < 0.05, truth = truth)
}
