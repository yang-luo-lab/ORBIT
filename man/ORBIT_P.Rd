% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ORBIT_P.R
\name{ORBIT_P}
\alias{ORBIT_P}
\title{Variance-Gamma Integration of Multi-Omic P-Values}
\usage{
ORBIT_P(
  omics_list,
  direction,
  rho = NULL,
  feature_col = "feature",
  sign_col = "sign",
  stat_col = "stat",
  seed = NULL
)
}
\arguments{
\item{omics_list}{Named list of data frames, one per omic. Each
must contain columns named by \code{feature_col}, \code{sign_col},
\code{stat_col}. Different omics may report different subsets of
features; the function works on the union. \code{stat_col} is
interpreted as a P-value in \verb{[0, 1]}.}

\item{direction}{Named numeric vector of +1/-1, names must match
\code{names(omics_list)}.}

\item{rho}{Between-omic correlation. One of:
\itemize{
\item \code{NULL} (default): auto-estimate a pairwise correlation matrix
using the same signed rank-based scoring as \code{ORBIT_cor()},
applied to all features. Skipping signal filtering is
acceptable here because P-value combination via \code{-log(P)} is
dominated by large signal magnitudes, making the combined
statistic relatively robust to slight \code{rho} misspecification —
more so than rank-based combination, where each feature's
contribution is capped at \code{log(m+1)}. This mirrors Empirical
Brown's Method, which also estimates correlation without
explicit filtering. For a cleaner estimate that excludes
significant features, run \code{ORBIT_cor()} and pass its
\verb{$rho_mat} here.
\item A single number in \verb{[0, 1]}. The same value is applied to
every feature. Negative user-supplied values are coerced to
\code{0} with a warning; values above \code{1} are an error; \code{rho = 1}
triggers a warning (VG null degenerates to Laplace).
\item A numeric square symmetric matrix with \code{rownames} / \code{colnames}
matching \code{names(omics_list)}. Validated and used exactly as
in \code{ORBIT_Rank()}'s matrix mode: diagonal must be \code{1},
off-diagonal must be strictly less than \code{1}, \code{NA} off-diagonal
entries allowed.
When \code{rho} is a matrix (user-supplied or auto-estimated), each
feature gets its own effective \code{rho} equal to the mean of the
upper-triangular entries of \code{rho} restricted to the omics in
which that feature is observed (its "presence pattern").
}}

\item{feature_col, sign_col, stat_col}{Column names. Defaults
\code{"feature"}, \code{"sign"}, \code{"stat"}. \code{feature} may be character or
factor; \code{sign} must be -1 or +1 (no NA); \code{stat} must be a
P-value in \verb{[0, 1]} (no NA). P-values equal to exactly \code{0} are
clamped to \code{.Machine$double.xmin} with a single aggregated
warning.}

\item{seed}{Optional seed for random tie-breaking in the rank
transform used by the auto-estimator. The global RNG state is
restored on exit.}
}
\value{
Data frame with columns:
\itemize{
\item \code{Feature}: feature identifier (union across omics).
\item \code{N}: integer, number of omics in which the feature is observed.
\item \code{P}: two-sided Variance-Gamma tail probability. \code{NA} when
\code{N == 0} or when the feature is skipped due to non-positive
effective inflation.
\item \code{Direction}: integer, \code{1L} if \code{D > 0}, \code{-1L} if \code{D < 0}, \code{NA}
if \code{D == 0} or \code{P} is \code{NA}.
The \code{rho} actually used (scalar or matrix) is attached as
\code{attr(result, "rho")} for inspection.
}
}
\description{
Combines per-feature P-values across omics using \code{-log(P)}
directly as the signed evidence score (no within-omic ranking).
Use this when you trust P-value magnitudes and consider them
comparable across omics. Otherwise feed the same P-values to
\code{ORBIT_Rank()}, which ranks within each omic and is invariant
to magnitude differences. Supports per-feature effective
between-omic correlation when \code{rho} is supplied as a matrix or
auto-estimated.
}
\details{
\strong{Combined statistic.} Per-feature signed score in omic \code{j} is
\code{sign * direction[j] * (-log(P))}. Under the null,
\verb{|score| ~ Exp(1)}. The combined statistic \verb{D = sum of scores} over
the \code{k = N} observed omics follows, under the equicorrelated
Laplace null with effective correlation \code{eff_rho}, a symmetric
Variance-Gamma with shape \code{r_eff = k / (1 + (k - 1) * eff_rho)}
and scale \code{sigma_eff = 1 + (k - 1) * eff_rho}. The tail probability
uses 32-node Gauss-Laguerre quadrature, with closed forms at
\code{r_eff = 1} (Laplace) and \code{r_eff = 2}. Features are grouped by
\verb{(N, eff_rho)} so the quadrature setup is reused.

\strong{Per-feature effective rho (matrix mode).} Each feature uses the
mean of upper-triangular \code{rho} entries restricted to its observed
omics. Single-omic features get \code{eff_rho = 0}. NA entries in \code{rho}
use available pairs (with \code{rho = 0} as fallback when all are NA).
Features whose \code{1 + (k - 1) * eff_rho} is non-positive are skipped
(\code{P = NA}).
}
\examples{
# 500 features, 80\% null, 20\% true signal, with non-zero null
# between-omic correlation so auto-estimate has something to recover.
sim <- ORBIT_simulate_signal(
  n = 500, K = 3,
  pi_null = 0.8, pi_up = 0.1, pi_down = 0.1,
  mu_signal = 2,
  rho_null = 0.3, rho_signal = 0.5,
  seed = 1
)
direction <- setNames(rep(1, 3), names(sim$omics_list))

# rho not supplied: auto-estimated as a matrix and attached
res <- ORBIT_P(sim$omics_list, direction)
attr(res, "rho")
head(res)

# Calibration sanity check against the simulated truth
fdr   <- p.adjust(res$P, method = "BH")
truth <- sim$is_signal[res$Feature]
table(predicted = fdr < 0.05, truth = truth)
}
