% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ORBIT_cor.R
\name{ORBIT_cor}
\alias{ORBIT_cor}
\title{Estimate Average Between-Omic Correlation from Per-Pair Non-Significant Features}
\usage{
ORBIT_cor(
  omics_list,
  direction,
  sig_mode = c("column", "thr"),
  sig_thr = NULL,
  stat_direction = c("smaller_better", "larger_better"),
  feature_col = "feature",
  sign_col = "sign",
  stat_col = "stat",
  sig_col = "significance",
  min_bg = 30L,
  bg_frac_tol = 0.5,
  seed = NULL
)
}
\arguments{
\item{omics_list}{Named list of data frames, one per omic. Each must
contain columns named by \code{feature_col}, \code{sign_col}, \code{stat_col}, and
(when \code{sig_mode = "column"}) \code{sig_col}.}

\item{direction}{Named numeric vector of +1/-1; names must match
\code{names(omics_list)}.}

\item{sig_mode}{\code{"column"} reads significance from \code{sig_col} (must
be 0/1, no NA). \code{"thr"} ignores \code{sig_col} and flags the
most-significant \code{floor(m * sig_thr)} features (minimum 1) per
omic, ranking by \code{stat} with random tie-breaking controlled by
\code{seed}. The direction of "most significant" is set by
\code{stat_direction}.}

\item{sig_thr}{Required when \code{sig_mode = "thr"}; a number in \verb{(0, 1)}.}

\item{stat_direction}{Convention for \code{stat_col}.
\code{"smaller_better"} (default) treats smaller \code{stat} as more
significant (p-value style); \code{"larger_better"} treats larger
\code{stat} as more significant (\verb{|t-stat|}, \code{-log10(p)}, enrichment
score, etc.). Either way \code{stat} must be non-negative; the sign of
the effect belongs in \code{sign_col}.}

\item{feature_col, sign_col, stat_col, sig_col}{Column names. Defaults
\code{"feature"}, \code{"sign"}, \code{"stat"}, \code{"significance"}. \code{sig_col} is
consulted only when \code{sig_mode = "column"}.}

\item{min_bg}{Minimum per-pair background size required to estimate
that pair. Pairs below are left as \code{NA} in \code{rho_mat}. Default \code{30}.}

\item{bg_frac_tol}{Threshold on \code{bg_frac = n_bg / n_overlap} used
for the low-bg-fraction warning. A pair is flagged when its
\code{bg_frac < bg_frac_tol}. Default \code{0.5}. Set to \code{0} to silence.}

\item{seed}{Optional seed for random tie-breaking. The global RNG
state is restored on exit.}
}
\value{
A list with:
\itemize{
\item \code{rho}: mean of the upper-triangle of \code{rho_mat} over valid
pairs (no trimming). \code{NA} if no pair has a finite correlation.
\item \code{rho_mat}: pairwise Pearson correlation on per-pair
background. Diagonal is \code{1}; entries are \code{NA} when the per-pair
background is below \code{min_bg} or the correlation is undefined.
\item \code{diag}: data frame with one row per unordered pair and
columns \code{omic_i}, \code{omic_j}, \code{n_overlap}, \code{n_bg}, \code{bg_frac},
\code{rho}, \code{tukey_dev}, \code{rho_loo}. Sorted by \verb{|tukey_dev|}
descending.
\item \code{n_total}: size of the feature union across omics.
\item \code{tukey_quartiles}: named vector \code{c(Q1, Q3, IQR)} of the
pair-correlation distribution; entries are \code{NA} when fewer than
four valid pairs exist.
\item \code{settings}: list of resolved values \code{min_bg} and
\code{bg_frac_tol}.
}
}
\description{
For each pair of omics, restricts to features that are present in
both omics and flagged as non-significant in both, recomputes ranks
within that subset, and forms a signed score whose Pearson
correlation across omics estimates the residual between-omic
correlation under the null. Returns the mean off-diagonal
correlation, the full pairwise correlation matrix, and a per-pair
diagnostic table.
}
\details{
\strong{Score construction.} Within each pair's background, the score
for a feature in omic \code{k} is \code{sign * direction[k] * (-log(adj_rank))},
where \code{adj_rank = r / (m_bg + 1)} and \code{r} is the rank of \code{stat}
arranged so that the most-significant feature has rank 1 (controlled
by \code{stat_direction}). Under the null \verb{|score|} is approximately
\code{Exp(1)}-distributed and the signed score is marginally
Laplace(0, 1).

\strong{Per-pair background.} For pair \verb{(i, j)} the background is the
set of features that are (a) observed in both omic \code{i} and omic \code{j},
and (b) have \code{sig = 0} in both omic \code{i} and omic \code{j}. There is no
cross-pair "global null" set: features that are significant in some
third omic \code{k} are unaffected as long as they are non-significant in
both \code{i} and \code{j}. Ranks \code{r} are recomputed \emph{within} each pair's
background before forming the score, so removing significant
features does not bias the rank distribution.

\strong{Aggregate \code{rho}.} \code{rho} is the arithmetic mean of the upper-
triangular finite pair correlations (no trimming applied).

\strong{Diagnostic table.} For each unordered pair, \code{diag} records:
overlap size (\code{n_overlap}), background size (\code{n_bg}), background
fraction (\code{bg_frac = n_bg / n_overlap}), Pearson correlation
(\code{rho}), a Tukey-IQR signed deviation (\code{tukey_dev}: distance to the
nearest IQR fence in IQR units, \code{0} if inside the box, sign
indicates direction; \verb{|tukey_dev| > 1.5} is the classical Tukey
outlier threshold but no automatic exclusion is applied), and a
leave-one-out \code{rho_loo} (mean of the other valid pair correlations,
or the overall mean of valid pairs for rows whose own correlation
is \code{NA}). Rows are sorted by \verb{|tukey_dev|} descending so the most
unusual pairs appear first. \code{tukey_dev} is \code{NA} when fewer than
four valid pairs exist or the IQR is zero; \code{rho_loo} is \code{NA} when
fewer than two valid pairs exist.

\strong{Messages and warnings.} The function emits two \code{message()}s
giving the per-pair background size summary (median, range, count)
and the value of \code{rho}. A single \code{warning()} is issued when any
pair has \code{n_bg < min_bg} or \code{bg_frac < bg_frac_tol}; specific pairs
are identified in \verb{$diag}.
}
\examples{
# Simulate data with real signal: 80\% null, 20\% true signal,
# rho_null = 0.3 (null between-omic correlation we want to recover),
# rho_signal = 0.5 (extra correlation among signal features).
sim <- ORBIT_simulate_signal(
  n = 500, K = 3,
  pi_null = 0.8, pi_up = 0.1, pi_down = 0.1,
  mu_signal = 2,
  rho_null = 0.3, rho_signal = 0.5,
  seed = 1
)

# Attach a `significance` flag per omic. In practice this is your
# DE call (e.g. FDR < 0.05); here we use the simulated truth.
omics <- sim$omics_list
for (j in seq_along(omics))
  omics[[j]]$significance <- sim$is_signal[omics[[j]]$feature]
direction <- setNames(rep(1, 3), names(omics))

fit <- ORBIT_cor(omics, direction, sig_mode = "column", seed = 1)
fit$rho           # recovers rho_null ~ 0.3 (signal filtered out)
fit$rho_mat       # pairwise matrix; feed into ORBIT_Rank / ORBIT_P
fit$diag          # per-pair diagnostics
}
